import type { NormalizedRecord } from "../../stages/03_normalize";
import { type HurlebatteSourceRecord } from "./parse";

export function normalize(
  record: HurlebatteSourceRecord,
  normalizedAt: string,
): NormalizedRecord | null {
  // todo
  return null;
}
